/**
 * Admin dashboard placeholder — replaced in Phase F5 with analytics/admin/,
 * the audit-log viewer, and the verification/report queues.
 */
export default function AdminHome() {
  return (
    <section className="card">
      <h1>Platform admin</h1>
      <p className="muted">
        Analytics, audit logs, and moderation queues arrive in Phase F5.
      </p>
    </section>
  );
}
